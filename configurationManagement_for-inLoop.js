const config = {
    host: "localhost",
    port: 3000,
    env: "development"
};

console.log("Server Configuration:");
for (let key in config) {
    console.log(`${key}: ${config[key]}`);
}

                                             
                                                                                                       